import React from 'react';
import Footer from './Footer';

export default { title: 'Footer' };

export const withSignInUp = () => <Footer></Footer>;

// storiesOf('Footer', module).add('withSignInUp', () => {
//   return <Footer></Footer>;
// });
